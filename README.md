Valentine Letter

Simple static site. To publish via GitHub Pages:

1. Create an empty repository on GitHub (do not initialize with README).
2. On your machine, run the git push commands provided by the assistant.
3. In GitHub repo Settings → Pages, set Source to `main` branch and `/ (root)`.
4. Your site will be available at https://<your-username>.github.io/<repo-name>/ within a few minutes.